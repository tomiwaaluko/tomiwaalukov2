
  # Cinematic Terminal Access Gate

  This is a code bundle for Cinematic Terminal Access Gate. The original project is available at https://www.figma.com/design/guhRbq7alykWkR73uNL5jj/Cinematic-Terminal-Access-Gate.

  ## Running the code

  Run `npm install` to install the dependencies.

  Run `npm run dev` to start the development server (Next.js).

  Run `npm run build` to create a production build.
  